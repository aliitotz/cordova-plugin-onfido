repositories {
  jcenter()

  maven {
    url  "https://dl.bintray.com/onfido/maven"
  }
}
dependencies {
  api 'com.onfido.sdk.capture:onfido-capture-sdk:5.2.0'

  api ('com.google.android.gms:play-services-base:+') {
      exclude group: 'com.android.support' // to avoid conflicts with your current support library
    }
}
